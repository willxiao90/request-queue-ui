# request-queue-ui

RequestQueue UI 界面，RequestQueue 源代码放在 `/src/lib` 目录下面，执行 `node ./src/lib/RequestQueue.test.js` 可以验证。

## 安装

执行 `yarn` 命令安装依赖。

## 启动

执行 `yarn start` 命令启动 UI 界面，打开 [http://localhost:3000](http://localhost:3000) 在浏览器查看。
